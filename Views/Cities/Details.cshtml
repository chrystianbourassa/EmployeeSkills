@model City

@{ ViewData["Title"] = "Détails sur une catégorie de compétence"; }

<div class="row text">
    <div class="col-md-8 offset-2">
        <p>
            <h1>Détail pour @Model.Name  </h1>
        </p>
        <div class="row">
            <div class="col-md-8 offset-2">
                <div class="form-group">
                    <label asp-for="CityID" class="control-label"></label>
                    <input asp-for="CityID" class="form-control" readonly />
                    <span asp-validation-for="CityID" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="Name" class="control-label"></label>
                    <input asp-for="Name" class="form-control" readonly />
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>

                <div class="form-group">

                    <a class="btn btn-outline-success float-right" asp-action="Edit" asp-route-id="@Model.CityID  ">Edit</a>
                    <a class="btn btn-outline-secondary" asp-action="Index">Annuler</a>
                </div>
            </div>
        </div>
    </div>
</div>

